source 'https://rubygems.org'
gem 'jekyll', '~> 4.2.0'

# If you're using a theme, include it as well
gem 'minima', '~> 2.5'  # Replace 'minima' with your theme's gem name if different

# Plugins
group :jekyll_plugins do
   gem 'jekyll-feed', '~> 0.15'
end
